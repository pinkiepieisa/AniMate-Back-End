services:
  - type: web
    name: animate-backend
    runtime: docker
    buildCommand: cd backend/backend && ./mvnw clean install -DskipTests
    startCommand: java -jar backend/backend/target/*.jar
    envVars:
      - key: JAVA_HOME
        value: /usr/lib/jvm/java-21-openjdk-amd64